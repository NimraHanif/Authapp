# Authapp
A React Native app demonstrating a full authentication flow with Redux Toolkit + Redux-Persist, separate Auth/App navigation stacks, JWT-based API auth with auto-logout on 401, and React Query for data fetching (using DummyJSON as the test API).
